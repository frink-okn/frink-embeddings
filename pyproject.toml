[project]
name = "frink-embeddings"
version = "0.1.0"
description = "Generate embeddings for RDF graph nodes."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.5",
    "qdrant-client==1.15.0",
    "rdflib==7.1.4",
    "rdflib-hdt==3.1",
    "sentence-transformers==4.1.0",
]
